/* FUNCTION OVERLOADING- multiplr functions with the same name but different parameters. 
Here, different parameters is on the basis of type and no. of parameters. */

/*#include<iostream>
using namespace std;

int sum(int a , int b){
    int s= a + b;
    cout << "sum is- " << s <<endl;
    return s;
}

double sum(double a , double b){
    cout << "the sum is- " << (a + b) << endl;
    return (a + b);
}

int sum(int a, int b, int c){
    cout<< " the su is- "<<(a + b + c)<<endl;
    return (a + b + c);
}

int main(){
    sum(2,6);
    sum(1.5, 2.5);  //decimal value is considered as double by the compiler and thus we get 3 in the output instead of 4. We need to define another function for this.
    sum(2, 6, 8);
    sum(1, 1.5, 7);         //function loading (has difference in terms of parameters.)
    return 0;
}           */

